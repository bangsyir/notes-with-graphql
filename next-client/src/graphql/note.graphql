query GetNotes {
  getNotes {
    id
    title
    description
  }
}

mutation AddNote($input: NoteInput) {
  addNote(input: $input) {
    errors {
      title
      description
    }
    note {
      title
      description
    }
  }
}
